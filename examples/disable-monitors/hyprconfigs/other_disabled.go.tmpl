{{- $someMonitor := index .MonitorsByTag "SomeMonitor" -}}

# auto generated by hyprdynamicmonitors

# disable 'ExtraMonitors' (not defined in the profile but currently present in `hyprctl monitors`)
{{- range .ExtraMonitors}}
monitor={{.Name}},disable
{{- end }}

# change to your preferred monitor config (see modes in `hyprctl monitors`)
monitor={{$someMonitor.Name}},2880x1920@120.00000,0x0,2.0,vrr,1
